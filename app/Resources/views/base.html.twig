<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        
        <script src="{{ asset("bundles/app/JS/jquery-3.1.1.js") }}" type="text/javascript"></script>
        
    </head>
    <body>
        <a href="{{ path('produit_index') }}">Produits</a>
        <a href="{{ path('categorie_index') }}">Categorie</a>
        <a href="{{ path('commande_index') }}">Commande</a>
        <a href="{{ path('client_index') }}">Client</a>
        <a href="{{ path('lister_produits') }}">Lister produits</a>
        <br>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
